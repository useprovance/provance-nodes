PORT=3100

# Node-specific secrets live in each node's own .env file:
#   nodes/goat/.env      — GOAT_PRIVATE_KEY, GOAT_RPC_URL etc.
#   nodes/blaze/.env     — STELLAR_AGENT_SECRET_KEY, SOROSWAP_API_KEY etc.
#   nodes/ai-decision/.env — OPENAI_API_KEY
#   nodes/telegram/.env  — TELEGRAM_BOT_TOKEN
